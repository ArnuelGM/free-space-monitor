# Usage

```txt
usage: free-space-monitor.py [-h] [--drive DRIVE] [--interval INTERVAL] [--threshold THRESHOLD] [--mode {foreground,background}]

Monitor de espacio en disco

options:
  -h, --help            show this help message and exit
  --drive DRIVE         Letra del disco a revisar.                  Default: C:\
  --interval INTERVAL   Intervalo en segundos.                      Default: 30
  --threshold THRESHOLD
                        Umbral en GB.                               Default: 10
  --mode {foreground,background}
                        Modo de ejecución: foreground o background. Default: background
```
